### Extensions
- Prettier
- Live server

### Shortcut keys
- Copy ->Ctrl +C
- Paste -> Ctrl + V 
- Cut -> Ctrl + X
- Save -> Ctrl + S
- Comment -> Ctrl + ? 
- Document format-> shift + alt + f
- Undo -> Ctrl + Z

### HTML -> Hypertext Markup Language
- structure for webpage via tags 

1. Text elements 
- `heading tags` `h1-h6`
- `p`
- `strong`
- `sup and sub`
- `em`

2. links and medias
- anchor tag -> `a`
- video tag -> `video`
- image tag -> `img`
- iframe -> `iframe`

3. Lists
- orderd lists -> `ol`
- unordered lists ->`ul`
- lists -> `li`

4. Table
- `table`
- `tr`
- `td`
- `th`
- `thead` , `tbody` , `tfoot`

5. Form
- action : route 
- method : Get , POST
- input: `text`,`number`,`email`,`tel`,`ratings`,`color`,`date`,`time`,`search`,`file`,`submit`
- `textarea`
- `select`

6. Miscllaneous
- `br`
- `hr`

7. inline and block elements

- Inline : they occupy the required width only `strong ,a , span`
- Block : they ocuupy full width of screen `div , h1-h6, p `


8. Semantics
- professional way of coding , rules
- All HTML elements must be in lowercase
- User proper tags according to their position
- Always fill `alt` attribute of image
- ALways format your code
- in a page there must be only one h1 tag , 
- h1 tag should be in every page

9. HTML structure
- `header`, `nav` , `main` , `section`, `div` , `footer`

### CSS-> Cascading Style Sheet
- colors, margin, padding , text-allign , font-size

***Syntax***

selector{
    property:value ;
}

**Usage**
- Inline 
- external 
- internal

**Selctors types**

1. Element = h1, p, span  -> lowest priority
2. Class = .nav-bar ->medium
3. Id   = #nav-bar -> Highest priority

Class Vs Id
- class - styling
- id- javascript

<!-- Variables -> container -> data ayera basxa -->
### CSS properties
1. Color, Background Color
2. Text
3. Font
4. Boxmodel : margin, padding, border
5. Display
6. Flex
7. Units
8. Pseudo class
9. Position  , Icons
10. Overflow
11. Transformation
12. Animation -> css
13. Grid -> css,tailwind
14. Responsive -> css, tailwind
===============================

CodeIt Header

### Tailwind Css

- CSS Framework 
- used for building UI rapidly
- we use predefined utility classes directly to HTML
- Highly customizable , works in darkmode
- JIT(Just in time)

1 Portfolio Website

***AOS***
-> Animate on Scroll

### JavaScript 
- programming language ra scripting language
- built by using C++
- set of instructions given to the computer
- EcmaScript -> javascript rulebook follow garney
- Es6

1. Variables 
2. Data Types
3. Operators
4. Conditional Statement
5. loop
6. Function
7. EcmaScript
- Template literals
- Spread operator , Rest operator
- Object/ Array Destructuring
- Arrow function

8. DOM (Document Object Model) Manipulation
- DOM represents HTML document as tree of objects
- DOM manipulation is a process of using JS to dynamically change content, structure , styling of webpage

9. Exception Handling